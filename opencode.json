{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["node_modules/.bin/playwright-mcp"],
      "args": [
        "--headless"
      ],
      "enabled": true,
      "env": {
        "BROWSER": "chromium"
      }
    }
  }
}
